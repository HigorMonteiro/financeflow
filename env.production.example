# ============================================
# .env para PRODUÇÃO na VPS
# ============================================
# Localização: /home/deploy/apps/finance_flow/.env
# Permissões: 600 (apenas leitura para o usuário deploy)
# ============================================
# COMO USAR:
# 1. Copie este arquivo para .env na VPS
# 2. Edite com suas credenciais reais
# 3. Configure permissões: chmod 600 .env
# ============================================

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Opção 1: SQLite (padrão - mais simples)
DATABASE_URL=file:./prisma/dev.db

# Opção 2: PostgreSQL (recomendado para produção)
# Descomente as linhas abaixo e comente a linha acima se usar PostgreSQL
# DATABASE_URL=postgresql://financeflow:SENHA_FORTE_AQUI@postgres:5432/financeflow
# POSTGRES_USER=financeflow
# POSTGRES_PASSWORD=SENHA_FORTE_AQUI_MINIMO_32_CARACTERES
# POSTGRES_DB=financeflow
# POSTGRES_PORT=5432

# ============================================
# BACKEND API CONFIGURATION
# ============================================
# JWT Secret - GERE UMA CHAVE FORTE (mínimo 32 caracteres)
# Use: openssl rand -base64 32
JWT_SECRET=GERE_UMA_CHAVE_FORTE_AQUI_MINIMO_32_CARACTERES_USE_OPENSSL_RAND_BASE64_32

JWT_EXPIRES_IN=7d
NODE_ENV=production
PORT=3000

# ============================================
# FRONTEND CONFIGURATION
# ============================================
# URLs de produção (substitua pelo seu domínio)
FRONTEND_URL=https://seu-dominio.com
CORS_ORIGIN=https://seu-dominio.com
CORS_CREDENTIALS=true

# URL da API para o frontend (pode ser o mesmo domínio ou subdomínio)
# Se usar subdomínio: https://api.seu-dominio.com
# Se usar mesmo domínio: https://seu-dominio.com/api
VITE_API_URL=https://seu-dominio.com/api

# ============================================
# PORT CONFIGURATION
# ============================================
# Portas expostas pelos containers
API_PORT=3000
WEB_PORT=80

# ============================================
# LOGGING
# ============================================
LOG_LEVEL=info
LOG_ERRORS=true

# ============================================
# OPTIONAL: Seed Data (apenas primeira vez)
# ============================================
# Descomente para criar usuário admin inicial
# SEED_EMAIL=admin@seu-dominio.com
# SEED_PASSWORD=senha_forte_admin
# SEED_NAME=Administrador

# ============================================
# OPTIONAL: Rate Limiting
# ============================================
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_REQUESTS=100

