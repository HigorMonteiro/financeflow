# Database Configuration
# Se não definido, usa SQLite por padrão: file:./prisma/dev.db
# Para usar PostgreSQL, defina DATABASE_URL como:
# DATABASE_URL=postgresql://financeflow:password@postgres:5432/financeflow
DATABASE_URL=file:./prisma/dev.db

# PostgreSQL Configuration (apenas se usar PostgreSQL)
# POSTGRES_USER=financeflow
# POSTGRES_PASSWORD=change_me_strong_password
# POSTGRES_DB=financeflow
# POSTGRES_PORT=5432

# Backend API Configuration
JWT_SECRET=change_me_generate_a_strong_secret_key_min_32_chars
JWT_EXPIRES_IN=7d
NODE_ENV=production
PORT=3000

# Frontend Configuration
FRONTEND_URL=http://localhost
CORS_ORIGIN=http://localhost
CORS_CREDENTIALS=true
VITE_API_URL=http://localhost:3000

# Port Configuration
API_PORT=3000
WEB_PORT=80

# Logging
LOG_LEVEL=info
LOG_ERRORS=true

# Optional: Seed Data
# SEED_EMAIL=admin@example.com
# SEED_PASSWORD=change_me
# SEED_NAME=Admin User

# Optional: Rate Limiting
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_REQUESTS=100
